# Web-thu-chi demo dùng postgresql + docker
# docker-compose down 
để xóa cache
# docker-compose build --no-cache
để build
# docker-compose up 
để chạy.
Dữ liệu docker chưa lưu sử dụng Volume để lưu dữ liệu. 
